const initialState = {};

const nonCachedReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

// Exports
export default nonCachedReducer;
